---
version: "v1"
deps:
  - "buf.build/envoyproxy/protoc-gen-validate"
  - "buf.build/googleapis/googleapis"
lint:
  except:
    - "ENUM_VALUE_PREFIX"
    - "ENUM_ZERO_VALUE_SUFFIX"

breaking:
  use:
    - "WIRE"